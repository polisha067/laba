apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - postgres.yaml
  - user-service.yaml
  - message-service.yaml
  - bff.yaml
  - frontend.yaml
  - migrations-users.yaml
  - migrations-messages.yaml
  - s3-csi.yaml
  - services.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/part-of: messenger